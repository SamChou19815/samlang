{
  "$schema": "https://turborepo.org/schema.json",
  "baseBranch": "origin/main",
  "globalDependencies": ["configurations/*"],
  "pipeline": {
    "compile": {
      "outputs": ["tsconfig.tsbuildinfo"]
    },
    "bundle": {
      "outputs": ["dist/index.js", "out/**", "bin/index.js", "bin/lsp.js"]
    }
  }
}
