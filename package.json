{
  "packageManager": "pnpm@8.3.1",
  "private": true,
  "scripts": {
    "test": "rm -f target/llvm-cov-target/*.profraw && cargo th",
    "build:samlang-wasm": "cd crates/samlang-wasm && ./setup-generated-package.sh"
  },
  "devDependencies": {
    "@dev-sam/samlang-cli": "workspace:0.8.0",
    "@types/node": "^20.3.0",
    "esbuild": "^0.18.0",
    "esbuild-plugin-wasm": "^1.1.0"
  }
}
