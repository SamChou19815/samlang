{
  "packageManager": "pnpm@8.7.4",
  "private": true,
  "scripts": {
    "test": "rm -f target/llvm-cov-target/*.profraw && cargo th",
    "build:wasm": "cd crates/samlang-wasm && ./setup-generated-package.sh"
  },
  "devDependencies": {
    "@types/node": "^20.3.0",
    "bun-types": "^1.0.1",
    "esbuild": "^0.18.0",
    "esbuild-plugin-wasm": "^1.1.0"
  }
}
